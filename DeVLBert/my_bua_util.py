import sys,os
ROOT_DIR = "nathan/DeVLBert"
def fix_path_bua():
    # print(f"Moving {ROOT_DIR} to end of sys.path")
    # dealt_with = False
    # i = 0
    # while i < len(sys.path) - 1:
    #     p = sys.path[i]
    #     if p.endswith(ROOT_DIR):
    #         p = sys.path.pop(i)
    #         if not dealt_with:
    #             sys.path.append(p)
    #             dealt_with = True
    #     else:
    #         i += 1
    # print(sys.path)
    return

def fix_path_detectron():
    # start_dir = os.getcwd()
    # print(f"Moving {start_dir} to end of sys.path")
    # dealt_with = False
    # i = 0
    # while i < len(sys.path) - 1:
    #     p = sys.path[i]
    #     if p.endswith(start_dir):
    #         p = sys.path.pop(i)
    #         if not dealt_with:
    #             sys.path.append(p)
    #             dealt_with = True
    #     else:
    #         i += 1
    return